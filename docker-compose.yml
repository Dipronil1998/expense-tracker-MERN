version: '3'
services:
  app:
    build:
      context: ./server
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env

  client:
    build:
      context: ./client
    ports:
      - "3000:3000"
